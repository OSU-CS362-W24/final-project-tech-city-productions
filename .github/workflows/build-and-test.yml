name : Build and test
on : [ push ] #workflow_call for reusability
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      # https://github.com/marketplace/actions/checkout
      - uses : actions/checkout@v3
        with:
          node-version: 20
      - run : npm ci
      - run : npm test
